---
title: Реализация
---

Заметки Dev об особенностях реализации: что было неочевидно при переносе спеки SA в код, какие
edge case'ы покрыты, какие расхождения со спекой приняты осознанно. Пишут `/nauta:dev`.

| Статья | Тема |
|--------|------|
| [personal-api-key-dev-notes.md](personal-api-key-dev-notes.md) | Реализация ядра авторизации, диспетчера профиля эндпоинтов, диагностики и пагинации для эпика «персональный API-ключ» (0.5.0, DEV-001) |
| [rooms-calendar-dev-a-notes.md](rooms-calendar-dev-a-notes.md) | Реализация комнаты/календаря/FR-19 (эпик «Комнаты, календарь, планирование», волна 2, DEV-A) |
| [rooms-calendar-dev-b-notes.md](rooms-calendar-dev-b-notes.md) | Реализация планирования встречи FR-13 (эпик «Комнаты, календарь, планирование», волна 2, DEV-B) |
| [rooms-calendar-dev-c1-notes.md](rooms-calendar-dev-c1-notes.md) | Предпросмотр боевой встречи без сети: доказательство нуля сетевых вызовов, состава полей тела и выдачи confirmation_id (боевая приёмка 0.6.0, DEV-001) |
| [rooms-calendar-dev-c2-notes.md](rooms-calendar-dev-c2-notes.md) | Разбор боевого 404 на `create-meeting-confirm`: встреча не создана, путь `/calendar` — вероятно без `/api`, различить «нет пути»/«нет объекта» без второго POST не удалось (боевая приёмка 0.6.0, DEV-002) |
| [rooms-calendar-dev-c3-notes.md](rooms-calendar-dev-c3-notes.md) | Разбор боевого 401 на `create-meeting-confirm` после правки пути на `/api/calendar`: встреча снова не создана, GET тем же токеном тем же путём работает, `sessionToken` уходит на POST без потерь, тело 401 не сохранено, сообщение «токен истёк» ложно (боевая приёмка 0.6.0, DEV-005) |
| [rooms-calendar-dev-d1-notes.md](rooms-calendar-dev-d1-notes.md) | DEV-007: третий боевой POST дал старое сообщение вместо ADR-008 — root cause: `httpx.AsyncClient` без `cookies=` копит `Set-Cookie` между запросами одного клиента, контрольный вызов диагностики (ADR-004) наследовал cookie отказавшего POST; правка — зачистка cookie-jar response-хуком |
| [rooms-calendar-dev-d2-notes.md](rooms-calendar-dev-d2-notes.md) | DEV-008: наблюдаемость диагностики — исход контрольного вызова, когда он тоже падает (атрибут `control_probe`), пустое тело как видимый факт, HTTP-код исходного отказа виден всегда; матрица текста сообщений 401/403 × контроль ОК/упал × тело пусто/непусто |
| [rooms-calendar-dev-e-notes.md](rooms-calendar-dev-e-notes.md) | DEV-009: тело и транспорт `create_meeting` по живому снимку DevTools — заголовки вместо query на мутирующей операции (`copy_remove_param`), состав тела 14 полей (`requiredAttendees`, `_FIXED`-литералы, условный `anonymousAccessExpirationDate`, `pinCode`-развилка), переименование CLI-флагов |
| [dev-010-contacts-and-cancel.md](dev-010-contacts-and-cancel.md) | DEV-010: резолюция контактов (`search_contacts`, ADR-010) и отмена встречи (`cancel_meeting`, ADR-011) по стабам QA-author — расщепление `auth.py` в `endpoints.py` (гейт C13), опечатка счёта символов в стабе квотирования, расширение регрессии «единственная мутирующая операция» на вторую |
| [dev-001-host-config-and-store.md](dev-001-host-config-and-store.md) | DEV-001 (волна 3, плагин ktalk): discovery `.ktalk.toml` (`host_config.py`), машинный дефолт хранилища и права `0700`/`0600` через процессный `umask` (`store.py`), явная copy-then-verify миграция (`store_migration.py`), четвёртый источник приоритета `resolve_db_path`, `ktalk config show` — 42 красных стаба QA-001 закрыты, `registry.py` не тронут |
| [dev-002-cli-mcp-coverage.md](dev-002-cli-mcp-coverage.md) | DEV-002 (волна 3, первая половина): матрица покрытия 15 MCP-инструментов подкомандами `ktalk` (ADR-012 §2а), 11 новых читающих подкоманд (`cli_content.py`, `cli_meetings_read.py`), вынос чанкинга транскрипта в общий слой `formatters.render_transcript_output` |
| [dev-002-plugin-deparametrization.md](dev-002-plugin-deparametrization.md) | DEV-002 (волна 3, вторая половина): перенос 11 файлов `.claude/` vault'а в отдельный репозиторий `ktalk-plugin`, депараметризация путей через `ktalk config show --json`, замена MCP-вызовов на CLI, `qmd` — опциональная интеграция (ADR-012 §5), `ktalk-processor.md` 436→201 строка |
| [dev-003-security-review-fixes.md](dev-003-security-review-fixes.md) | DEV-003 (волна 3): закрытие security review SEC-003 перед сборкой плагина — BLOCK-01/MIN-01 атомарная миграция с явными правами (`store_migration.py`), MAJ-01 wiring `.ktalk.toml` в `cli.py::main`, MAJ-02 сужение окна `umask` до конструктора `Registry`, MAJ-03 чинит существующий корень хранилища, MAJ-04 CLI-команда `migrate-to-central-store`, MIN-02/INFO-01/INFO-02 — принятые остаточные риски |
| [dev-004-plugin-assembly.md](dev-004-plugin-assembly.md) | DEV-003 (волна 3, сборка): `plugin.json`/`.mcp.json`/marketplace-манифест по прецеденту `nauta`, README (предусловие PATH, `.ktalk.toml`, хранилище ADR-013, `migrate-to-central-store`), статическая проверка состава `scripts/check-plugin-composition.sh` в дереве плагина — открытый вопрос владельца: URL git-репозитория ktalk-plugin |
| [dev-005-onboarding.md](dev-005-onboarding.md) | DEV-005 (волна 4): санкционированный онбординг ktalk-mcp по ADR-014 — `ktalk --version`, `compat.json`, санкция `status`/`grant`/`revoke` (fail-closed, TTY на `grant`), `install` с ретраем и `--json`, врезки в 4 промта плагина; расхождения со спекой и открытые пункты владельца (публикация 0.7.0 в PyPI) |
| [dev-007-onboarding-defects.md](dev-007-onboarding-defects.md) | DEV-007 (волна 5): два дефекта онбординга, запаркованных финальным ревью волны 4 — `install`/`check` проверяли разные предикаты (постусловие `install` выровнено на предикат `check`), `install --json` не нёс телеметрию `attempts`/`return_code`/`uv_output` на исходах без запуска менеджера (код выровнен на спеку) |
| [dev-009-cli-json-and-exit-codes.md](dev-009-cli-json-and-exit-codes.md) | DEV-009 (волна 5): `--json` добавлен на `create-meeting-preview`/`cancel-meeting-preview`/`search-contacts` (без `confirmation_id` — ADR-015, не переживает границу процессов), `*-confirm` намеренно без `--json` (TTY-only); `search-contacts` разведён на три кода возврата (0 найден/1 отказ/2 не найден вместо общего `1`); два стаба QA-005 перевёрнуты осознанно, точечная правка `ktalk-plugin-meetings-spec.md` |
| [dev-010-meetings-surface-tests.md](dev-010-meetings-surface-tests.md) | DEV-010 (волна 5): 15 оставшихся стабов QA-005 в `tests/test_cli_meetings_surface.py` закрыты без единой правки `src/` — все проверенные CLI-факты (`list-calendar`, `get-room`, `cancel-meeting-preview`/`-confirm`, эскалация без реестра, проброс текста ошибки) уже соответствовали AC; единственная деталь — мок контрольного GET ADR-007 |
| [dev-006-plugin-meetings-surface.md](dev-006-plugin-meetings-surface.md) | DEV-006 (волна 5, репозиторий `ktalk-plugin`): новый навык `skills/ktalk-meetings/SKILL.md` (шесть секций FR-32…FR-38), расширение `check-plugin-composition.sh` на NFR-20, версия плагина 1.1.0; версионный узел закрыт — пакет `ktalk-mcp` поднят до 0.8.0, `compat.json` — `ktalk_mcp_min_version: 0.8.0`, публикация в PyPI не выполнена (операция владельца) |
| [dev-011-version-drift.md](dev-011-version-drift.md) | DEV-011 (волна 5): `__version__` остался литералом 0.7.0 при дистрибутиве 0.8.0 — гейт совместимости плагина читает `ktalk --version` и браковал бы совместимую установку; версия переведена на метаданные дистрибутива, добавлен сторож против расхождения с `pyproject.toml`, выпущена 0.8.1 |
| [dev-012-write-sanction-and-journal.md](dev-012-write-sanction-and-journal.md) | DEV-012 (волна 6): реализация ADR-016 в пакете — санкция записи со сроком и бюджетом, персистентный `ConfirmationStore`, журнал операций, расщепление `cli_meeting.py`; две находки — `confirmation_id` мог начаться с дефиса и ломал разбор командной строки, `status_code` терялся на ветке «контроль прошёл» и портил классификацию исхода в журнале |
| [dev-013-calendar-inclusive-end.md](dev-013-calendar-inclusive-end.md) | DEV-013 (волна 7): включительная правая граница окна календаря (ADR-017, FR-39) — компенсация серверной полуоткрытости `+1 день` в `_fetch_segment`, отказ `start > end` до сети в `split_window`; маркер `assert_all_responses_were_requested=False` добавлен в стаб AC-5 (без него сценарий физически не проходим), соседний тест на известный 400-текст переключён с обратного окна на валидное |
| [dev-014-analysis-calibration.md](dev-014-analysis-calibration.md) | DEV-014: калибровка промт-слоя анализа плагина `ktalk` по ADR-018 (FR-40…FR-43, NFR-25) — шаг 4.5 финальной сверки прозы с таблицей «Договорённости», маркер `[ASR?]` и опциональная секция «Ключевые тезисы» в шаблоне протокола, пакетный резолвинг третьих лиц с кешем, примеры 3а/3б калибровки confidence, гейт NFR-25 в `check-plugin-composition.sh` (репозиторий `ktalk-plugin`), доказан обоими исходами |
| [dev-015-prompt-defect-channel.md](dev-015-prompt-defect-channel.md) | DEV-015: канал дефектов промта по ADR-019 (FR-44…FR-47, NFR-26, NFR-27), урезанный объём без скрипта/санкции/журнала — `CONTRIBUTING.md` и каталог `defect-classes.md` (репозиторий `ktalk-plugin`), секция «Дефекты промта» и правило таблицы `Defects` в `ktalk-evaluator.md`, обязательный предпросмотр в `SKILL.md`; порог 2+ и обезличивание — правило текста, не кода, не проверены живым прогоном агента; довесок — недостающий файл `.gitlab/issue_templates/Дефект промта.md` |
| [dev-016-silence-not-acceptance.md](dev-016-silence-not-acceptance.md) | DEV-016: регрессия A/B-прогона — шаг 4.5 (`ktalk-processor.md`, репозиторий `ktalk-plugin`) толкал молчание/«угу»/«не возразил» в строки «Ключевые решения»/«Договорённости»; явное исключение в шаге 4.5, адресация запрета §6 `analysis-quality.md` к этому шагу, пример 3в |
| [dev-017-timezone-format-validation.md](dev-017-timezone-format-validation.md) | DEV-017: локальная проверка формата `--timezone` (ADR-020, FR-40) — `TimezoneFormatError`/`_TIMEZONE_RE` в `build_meeting_body`, перехват в обеих CLI-ветках, `--help`/докстринг; мина фикстур `Europe/Moscow` шире заявленных QA четырёх файлов — фактически семь, правлены на `GMT+3` |
| [dev-018-session-token-file.md](dev-018-session-token-file.md) | DEV-018: файл `~/.config/ktalk-mcp/token` как третий источник авторизации после ключа и переменной окружения (`token_file.py`, валидатор в `Settings`, `ktalk token set|status`) — тождество токена из `localStorage` рабочему проверено живьём и хешем; находка: `token set` принимал любое значение и записал путь к файлу из устаревшего буфера, добавлена проверка формата |
| [dev-019-open-issues-identity-sync-null-fallback.md](dev-019-open-issues-identity-sync-null-fallback.md) | DEV-019: 22 стаба QA-013 по ADR-023 закрыты — `transcript_identity.py` (NFR-17, сверка включена по умолчанию, `--no-verify-identity` отключает), `last_synced` в `dashboard --json` (FR-41), `.get(id, fallback)` → `.get(id) or fallback` (issue #3); два существующих теста `test_cli_content.py` получили `--no-verify-identity` — не относились к предмету NFR-17 |
| [dev-020-open-issues-wave2-loud-mismatch-and-chunk-signal.md](dev-020-open-issues-wave2-loud-mismatch-and-chunk-signal.md) | DEV-020: 8 стабов QA-author волны 2 по ADR-024 (Д1/Д3) закрыты — `resolve_chunk_range` (новая чистая функция `formatters.py`, переиспользует чанкинг `render_transcript_output`), `cmd_get_transcript` завершается кодом 3 на `mismatch`, `--chunk` вне диапазона не оплачивает сеть на сверку и отдаёт `not_checked`/`chunk_out_of_range` валидным JSON; обе точки решения доказаны мутацией, версия 2.1.0 |
