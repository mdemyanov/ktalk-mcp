---
title: Календарь Контур.Толк — параметры чтения и форма тела создания встречи
properties:
  - name: Тип контента
    value: [Исследование]
  - name: Фаза
    value: [Production]
  - name: Статус
    value: [Draft]
---

**Дата:** 2026-08-13
**Исследователь:** researcher-agent
**Запрос PM/BA:** закрыть §5 п.1-2 постановки волны 2 (`docs/postanovka-wave-2-rooms-calendar.md`) —
точные параметры `GET /api/calendar` (метод: перебор read-only GET по текстам ошибок валидации) и
форма тела `POST /calendar` (метод: документарный анализ спеки/чужого кода — POST на боевой домен
запрещён красной линией разведки).
**Глубина:** standard (≈2ч: живое зондирование §1 + разбор OpenAPI §2)

Нумерация продолжает прошлые волны (RES-001 `ktalk-integrator-api.md` заняла Ф-1…Ф-16 внутри
`personal-api-key.md`; здесь — Ф-17…Ф-44).

## TL;DR

**П.1 закрыт полностью живыми запросами.** `GET /api/calendar` принимает `start` (обязателен),
`end`, `take`, `roomName`; `startFrom`/`from`/`dateFrom`/`startDate`/`top`/`count`/`limit`/`skip`/
`query` — либо не распознаются, либо принимаются синтаксически, но не влияют на выдачу. Жёсткий
лимит диапазона 7 дней действует только при явном `start`+`end`; при `start`+`take` сервер сам
расширяет окно вперёд без ограничения. `take` формально 1–1000, фактически режется на 100 —
тот же паттерн, что у `top` на `/api/recordings` (см. CLAUDE.md). Пагинации `skip`-подобной нет.
Ответ — `{"items": [...]}`, без курсора, отсортирован по возрастанию времени начала.

**П.2 закрыт документарно, с явной пометкой достоверности.** Прямого пути к схеме `POST /calendar`
нет — в спеке эндпоинта вообще нет. Ближайший документированный аналог — `CreateEmailCalendarEventModel`
для `POST /api/EmailCalendar/{email}` (другой, api-key-only путь). Его состав почти 1:1 покрывает
сигнатуру `create_meeting` из mainpart после перевода snake_case→camelCase — это совпадение по двум
независимым источникам укрепляет доверие к общему подмножеству полей, но не подтверждает их для
`/calendar` напрямую. Схема заметно шире сигнатуры mainpart: там, где mainpart предлагает только
булев `is_recurring`, документированная модель требует полноценный объект `recurrence` (тип
периодичности, интервал, дни недели) — без него неизвестно, что делает `isRecurring: true`.

**Неожиданная находка, меняющая допущение постановки:** ключ без нужных scope (`ktalk auth-status`
сообщает о нехватке `application.applications.read`) получает **200 с реальными данными** на
недокументированном `GET /api/calendar`, при этом 403 — на документированном
`GET /api/EmailCalendar/{email}` и на кандидате «whoami» `GET /api/domain/applications/access-info`.
То есть календарь **частично доступен в api-key-режиме уже сейчас** — вопреки допущению постановки
«ключу не хватает прав вообще ни на что в этой волне». Требует внимания SA при проектировании
`OPERATION_PROFILES` (Ф-34).

**П.3 (идемпотентность повторного `POST /calendar`) не исследовался** — физически не проверяется
GET-запросами, вынесен в боевую приёмку с ведома владельца, как и предписывает постановка.

## Ключевые находки

1. `start` — рабочее имя параметра начала окна `GET /api/calendar`; альтернативные варианты имени
   не распознаются — Ф-17. [primary] [established]
2. Жёсткий лимит 7 дней действует только при явном `end`; в режиме `start`+`take` окно не
   ограничено — Ф-20. [primary] [established]
3. `skip` и `query` принимаются без ошибки, но не влияют на выдачу; `roomName`, напротив, реально
   фильтрует — доказано сравнением count — Ф-22…Ф-24. [primary] [established]
4. `start`/`end` НЕ игнорируются молча (в отличие от `/api/recordings`, CLAUDE.md) — доказано
   нулевой выдачей на заведомо пустых окнах и ненулевой на дальнем будущем с известными
   повторяющимися встречами — Ф-25. [primary] [established]
5. Ключ без нужных scope получает 200 на недокументированном `/api/calendar`, но 403 — на
   задокументированном аналоге и на кандидате whoami — Ф-33…Ф-35. [primary] [established, но
   причина (какой scope в действительности проверяется) не выяснена]
6. Строка `"Разрешения: application.сalendar.write"` в спеке — не обрезана до `"application."`,
   как предполагала постановка, а содержит кириллический омоглиф `с` вместо латинского `c` —
   Ф-36. [primary] [established]
7. Документированный аналог тела создания встречи (`CreateEmailCalendarEventModel`) почти 1:1
   совпадает с сигнатурой `create_meeting` из mainpart, но заметно шире — Ф-37…Ф-39. [primary +
   secondary, скрещено] [emerging — совпадение, не прямое подтверждение]

## §1. `GET /api/calendar` — параметры (проверено живыми GET, session-режим)

### Метод

Все запросы — `env -u KTALK_PERSONAL_API_KEY` + `httpx.get(..., params={..., "sessionToken": <REDACTED>})`,
без заголовка `X-Auth-Token`. Проверено программно на каждый запрос: `sessionToken=` присутствует в
итоговом URL, `X-Auth-Token` в заголовках запроса отсутствует. Значения токена/ключа нигде не
печатались и не попали в этот документ.

### Ф-17. Имя параметра начала окна — `start`

```
GET /api/calendar?start=2026-08-13&sessionToken=<REDACTED>
→ 400 {"errorMessage":"Должна быть задана или дата окончания, или количество запрашиваемых
        элементов; Должна быть задана или дата окончания, или количество запрашиваемых элементов"}
```

Проверенные и отвергнутые варианты имени (все дают тот же 400 «Дата начала является обязательной
для заполнения», что и полное отсутствие параметров — то есть не распознаются вообще):
`startFrom`, `from`, `dateFrom`, `startDate`. Формат — как короткая дата (`2026-08-13`), так и
полный ISO-8601 с `Z` (`2026-08-13T00:00:00.000Z`) принимаются одинаково (200, тот же набор
элементов). [primary] [established]

### Ф-18. Имя параметра конца окна — `end`

```
GET /api/calendar?start=2026-08-13&end=2026-08-14&sessionToken=<REDACTED>
→ 200 {"items":[...10 элементов...]}
```

[primary] [established]

### Ф-19. Имя параметра количества — `take`

`top`, `count`, `limit` не распознаются — с ними сохраняется ошибка «нужен `end` или количество».
`take` — рабочее имя (совпадает с именем параметра у задокументированного аналога
`GET /api/EmailCalendar/{email}`, см. §2). [primary] [established]

### Ф-20. Разное поведение лимита диапазона в зависимости от комбинации параметров

| Комбинация | Поведение |
|---|---|
| `start` + `end`, диапазон ровно 7 дней (`08-01`→`08-08`) | 200, 26 элементов |
| `start` + `end`, диапазон 8 дней (`08-01`→`08-09`) | 400 «Период запроса не должен превышать 7 дней» |
| `start` + `take` (без `end`) | 200 в любом протестированном случае; `take=100` вернул элементы
  с разбросом дат более месяца (`2026-08-03` … `2026-09-07`) — лимит 7 дней здесь **не
  применяется** |

[primary] [established]

### Ф-21. `take` валидируется на 1..1000, фактически режется на 100

```
take=1001 → 400 "The field Take must be between 1 and 1000."
take=0    → 400 "The field Take must be between 1 and 1000."
take=-1   → 400 "The field Take must be between 1 and 1000."
take=101  → 200, count=100
take=1000 → 200, count=100
```

Тот же паттерн, что у `top` на `/api/recordings` (CLAUDE.md: «`top` максимум 100, не 1000»):
валидационная граница схемы и фактический потолок выдачи — разные числа. [primary] [established]

### Ф-22. `skip` принимается, но не работает

```
start=2026-08-01&take=2&skip=0 → первый элемент start=2026-08-03T07:00:00Z
start=2026-08-01&take=2&skip=2 → первый элемент start=2026-08-03T07:00:00Z (тот же!)
```

Нет ошибки валидации, нет и эффекта — способа получить вторую «страницу» сверх первых `take`
элементов через `skip` не существует. [primary] [established]

### Ф-23. `roomName` реально фильтрует (доказано сравнением, не предположено)

Базовая выборка за 7-дневное окно — 26 элементов, 26 различных комнат (1:1). Тест: взят реальный
`roomName` из выборки (не публикуется — внутренний идентификатор), передан фильтром:

```
count без фильтра        = 26
count с реальным roomName = 1, и все вернувшиеся элементы совпадают по этой комнате
count с несуществующим roomName ("definitely-not-a-real-room-xyz") = 0
```

[primary] [established]

### Ф-24. `query` принимается синтаксически, но не фильтрует

```
query=zzz_no_such_meeting_zzz (заведомо не встречающаяся строка) → count=26, то же, что без фильтра
```

В отличие от `roomName`, `query` не даёт эффекта — либо параметр не реализован для этого
эндпоинта, либо ищет не по тем полям, что были протестированы. [primary] [established, в
формулировке «эффекта не обнаружено при тестированном значении», не «доказано, что параметр
всегда игнорируется»]

### Ф-25. `start`/`end` не клэмпятся молча — окно реально применяется в обе стороны

В отличие от `/api/recordings`, где `startFrom`/`startTo` игнорируются (CLAUDE.md), здесь окно
проверено доказательно:

```
start=2020-01-01, end=2020-01-02  → count=0   (окно в прошлом, до появления данных Толка)
start=2020-01-01, take=5          → count=0   (не клэмпится к «сейчас» — иначе вернул бы
                                                ближайшие будущие элементы)
start=2027-06-01, end=2027-06-02  → count=0   (окно в будущем без плановых встреч)
start=2030-01-01, take=5          → count=5, элементы с start в диапазоне 2030-01-02…2030-01-03
                                     (реальные повторяющиеся встречи, запланированные на годы вперёд)
```

[primary] [established]

### Ф-26. Полный набор текстов валидации, собранных зондом

| Условие | `errorMessage` (дословно) |
|---|---|
| нет `start` | «Дата начала является обязательной для заполнения» |
| есть `start`, нет `end` и нет `take` | «Должна быть задана или дата окончания, или количество запрашиваемых элементов» (дублируется дважды подряд в одном сообщении — вероятно, накладываются два правила валидации с одинаковым текстом) |
| `end` - `start` > 7 дней | «Период запроса не должен превышать 7 дней» |
| `end` < `start` | «Дата окончания должна быть больше даты начала» |
| `start` не парсится как дата | «The value '...' is not valid for Start.» (внутреннее имя поля модели — `Start`, с заглавной буквы) |
| `take` вне 1..1000 | «The field Take must be between 1 and 1000.» |

[primary] [established]

### Ф-27. Форма ответа: `{"items": [...]}`, без пагинации

Единственный ключ верхнего уровня — `items`. Нет `total`/`count`/`nextPageToken`/`hasMore`.
Совпадает 1:1 с задокументированной схемой `EmailCalendarResult` (§2). [primary] [established]

### Ф-28. Форма элемента: близка к `EmailCalendarItem`, но не идентична

Ключи, реально встретившиеся хотя бы раз в выборке из 100 элементов (объединение):
`busyType, calendarSource, description, end, id, isRecurring, location, locationAttendee, meetId,
onlineMeetingUrl, onlineUsers, optionalAttendees, organizer, requiredAttendees, room, roomName,
start, stream, subject, urlParams`.

- Два поля отсутствуют в задокументированной `EmailCalendarItem` вовсе: **`meetId`**, **`urlParams`**.
- Среди 100 элементов (74 с `isRecurring: true`) ни разу не встретились документированные поля
  `recurrence`, `isCancelled`, `isAllDayEvent`, `externalMeeting`, `isPrivate`, `onlineUsersCount` —
  даже там, где `isRecurring: true` подразумевает наличие `recurrence` по описанию соседней
  операции (`EmailCalendar_GetRecurrenceEventById`, см. §2).
- `room` внутри элемента — тот же набор полей, что задокументирован для FR-17 («Комната») в
  постановке (`roomName, sessionHalls, stageConferenceId, moderators, ...`), подтверждает связку
  Комната↔Встреча на уровне ответа календаря, не только по имени.

Вывод: внутренний `/api/calendar` — не гарантированно та же реализация, что задокументированный
`/api/EmailCalendar/{email}`, а лишь схожая по форме. [primary] [established по фактам выдачи;
причина расхождения — не то же ли это в бэкенде — не выяснена]

### Ф-29. Сортировка — по возрастанию времени начала

`take=1` → элемент с наименьшим `start` в окне; `take=3` → три элемента строго по возрастанию
`start`. Противоположно `/api/recordings`, где порядок — от новых к старым (Ф-16, прошлая волна).
[primary] [established]

### Ф-30. Семантика выдачи шире, чем «личный календарь вызывающего»

За 7-дневное окно текущая сессия увидела встречи **26 разных организаторов** (не одного — не
похоже на «мой личный календарь»), и ровно 26 разных комнат на 26 элементов (1:1 встреча↔комната).
Правдоподобное прочтение: `/api/calendar` — календарь бронирований комнат Толка в целом (или как
минимум видимых текущей сессии комнат), а не персональный Outlook-календарь вызывающего
пользователя. Проверить с менее привилегированной сессией не удалось — доступен только один
аккаунт для зонда. [primary] [emerging — интерпретация правдоподобна, но не проверена вторым
аккаунтом]

### Ф-31. `calendarSource` — 100% `"ews"` во всей выборке

Подтверждает, что календарь этого пространства Толка интегрирован с Exchange (см. также
`POST /api/Calendar/servers` в §2: «Основным календарем пространства должен быть календарь EWS или
Exchange Online»). [primary] [established]

## §1b. Взаимодействие с режимом авторизации

### Ф-32. Задокументированный аналог `/api/EmailCalendar/{email}` под session-режимом — 401

```
GET /api/EmailCalendar/<self-email>?start=2026-08-13&end=2026-08-14&sessionToken=<REDACTED>
→ 401, пустое тело
```

Подтверждает известный паттерн (CLAUDE.md): интеграторские/задокументированные пути полностью
отвергают sessionToken, а не просто требуют другой scope. [primary] [established]

### Ф-33. Тот же путь под api-key-режимом (недостаточный по правам ключ) — 403

```
GET /api/EmailCalendar/<self-email>?start=2026-08-13&end=2026-08-14
Заголовок: X-Auth-Token: <REDACTED>
→ 403, пустое тело
```

Ожидаемо по спеке (`Разрешения: application.сalendar.read`, см. Ф-36 про опечатку в имени).
[primary] [established]

### Ф-34. Внутренний `/api/calendar` под тем же ключом — 200 (!)

```
GET /api/calendar?start=2026-08-13&end=2026-08-14
Заголовок: X-Auth-Token: <REDACTED>  (тот же ключ, что дал 403 выше)
→ 200, count=10 (то же число элементов, что под сессией для того же окна)
```

Негативные контроли на том же пути и том же окне:

```
X-Auth-Token: 00000000-0000-0000-0000-000000000000 (заведомо неверный) → 401
без заголовка авторизации вовсе                                        → 401
```

То есть путь реально проверяет валидность ключа (невалидный/пустой → 401), но не блокирует
известный по `ktalk auth-status` недостаточный по правам ключ. Значит доступ к недокументированному
`/api/calendar` в api-key-режиме **есть уже сейчас**, несмотря на то, что документированный аналог
и кандидат whoami тем же ключом получают 403. Требуемый (если вообще есть) scope для этого
конкретного пути не выяснен — контраст с Ф-33/Ф-35 показывает, что три родственных операции
проверяют доступ по-разному. [primary] [established сам факт; причина — открытый вопрос]

### Ф-35. Кандидат whoami из прошлой волны под тем же ключом — 403 (пересматривает [emerging] на противоположный установленный факт)

```
GET /api/domain/applications/access-info
Заголовок: X-Auth-Token: <REDACTED>
→ 403, пустое тело
```

RES-001 (`ktalk-integrator-api.md`, находка 8) отмечала этот путь как вероятного «whoami без
scope» — вывод помечался `[emerging — нужна проверка живым запросом]`. Проверка проведена: для
данного ключа эндпоинт **требует** какое-то условие, которого у ключа нет (403, не 200). Прежнюю
гипотезу считать опровергнутой для данного ключа; статус находки RES-001 №8 — пересмотреть на
`[established, отрицательный результат]`. Возможно, дело не в отсутствии scope как такового, а в
каком-то ином необходимом условии (например, типе ключа) — при текущих данных различить нельзя.
[primary] [established]

### Ф-36. Строка разрешения в спеке — не обрезана, а содержит кириллический омоглиф

Постановка со слов владельца утверждала: «у него строка разрешения обрезана до `application.`».
Побайтовая проверка (`unicodedata.name` по каждому символу) показала обратное: строка полная —
`"Разрешения: application.сalendar.write"` (и аналогично `...read` у GET), но 13-й символ слова
`calendar` — `с` (U+0441, CYRILLIC SMALL LETTER ES), не `c` (U+0063, LATIN SMALL LETTER C).
Проверено на обеих операциях (`EmailCalendar_GetAll`, `EmailCalendar_Create`) — опечатка
идентична в обеих. Реальное имя scope в перечислении `TalkScopeType` (RES-001) — `calendar`
(латиницей). Вероятный источник впечатления «обрезано»: ASCII-ориентированный парсер строки
разрешения (например, регэксп с классом `[a-z]` вместо unicode-класса) на кириллическом символе
не матчит остаток слова и визуально даёт «application.» — это гипотеза о причине наблюдения
постановки, не опровержение самого наблюдения владельца. [primary] [established — сам факт
опечатки; гипотеза о причине «обрезанности» — правдоподобное объяснение, не проверено]

## §2. `POST /calendar` — форма тела (документарный анализ, POST на боевой домен не выполнялся)

Красная линия разведки исключает любые пробные записи. Раздел целиком построен на трёх источниках:
1. `talk.public.api-api-2.json` — схема `CreateEmailCalendarEventModel` для **другого** пути
   (`POST /api/EmailCalendar/{email}`, api-key-режим, требует email организатора в пути);
2. `content/10-domain/research/mainpart-ktalk-mcp.md` [secondary] — сигнатура python-функции
   `create_meeting` из чужого MCP-сервера, вызывающей `POST /calendar` (внутренний, session-режим,
   без email в пути) — тот самый путь, который нужен волне;
3. Тексты ошибок валидации `GET /api/calendar` (§1) — не описывают тело POST напрямую, но дают
   независимое подтверждение имён `start`/`end`/`take`/`roomName`, что снижает вероятность
   расхождения именования между чтением и записью в рамках одного и того же внутреннего пути.

Прямого пути к самой схеме `POST /calendar` в спеке нет — это осознанный пробел, а не результат
плохого поиска: раздел «Ф-43» ниже фиксирует, что весь набор Calendar-схем компонентов исчерпан.

### Ф-37. Ближайший документированный аналог — `CreateEmailCalendarEventModel`

Обязательные поля по схеме: **`requiredUserKeys`, `roomName`, `subject`**. Остальные объявлены,
но не входят в `required`:

| Поле | Тип | Описание в спеке |
|---|---|---|
| `start` | string, date-time | Дата начала встречи |
| `end` | string, date-time | Дата окончания встречи |
| `timezone` | string | «Временная зона в формате GMT+X» |
| `description` | string | Описание встречи |
| `allowAnonymous` | boolean | Разрешить присутствие внешних пользователей |
| `enableSip` | boolean | Включить SIP |
| `pinCode` | string | Пин-код комнаты |
| `enableAutoRecording` | boolean | Автозапись |
| `isRecurring` | boolean | Признак повторяющейся встречи |
| `recurrence` | object `TalkCalendarRecurrence` | Шаблон ряда (см. Ф-39) |
| `simultaneousTranslation` | object | `{enabled: bool, languagePairs: [...]}` |
| `customMeetingUrl` | string | Пользовательский URL |
| `maskingSettings` | object | `{nameMaskingMode, postMaskingMode, showAdditionalInfo}` |
| `autoRunDeepFakeDetection` | boolean, nullable | Автозапуск детекции дипфейков |
| `isAllDayEvent` | boolean | Регулярная встреча (так в тексте спеки — по названию поля скорее «на весь день») |
| `optionalUserKeys` | array of string | Необязательные участники (по ключу пользователя) |
| `requiredExternalAttendeesEmails` | array of string | Обязательные внешние участники (по email) |
| `optionExternalAttendeesEmails` | array of string | Необязательные внешние участники (по email) |
| `controlledViaExternalSystem` | boolean, nullable | Встреча управляется внешней системой |

`additionalProperties: false` — сервер, судя по этой модели, отклонит неизвестные поля тела
(строгая схема, не permissive). [primary] [established для модели `/api/EmailCalendar/{email}`;
для `/calendar` — экстраполяция, см. Ф-38]

### Ф-38. Сопоставление с сигнатурой `create_meeting` из mainpart

`create_meeting(subject, start, end, room_name=None, timezone="GMT+3", required_attendees=None,
enable_sip=True, pin_code=None, enable_auto_recording=False, is_recurring=False, description="")`
[secondary, mainpart].

| Python-параметр mainpart | JSON-поле `CreateEmailCalendarEventModel` | Совпадение |
|---|---|---|
| `subject` | `subject` | имя совпадает |
| `start` | `start` | имя совпадает |
| `end` | `end` | имя совпадает |
| `room_name` | `roomName` | camelCase-перевод, совпадает по смыслу |
| `timezone` | `timezone` | имя и **формат значения** совпадают (Ф-41) |
| `required_attendees` | `requiredUserKeys` | camelCase, но заметно разные слова (`attendees` vs `userKeys`) — совпадение по позиции и семантике, не по написанию |
| `enable_sip` | `enableSip` | camelCase-перевод, совпадает |
| `pin_code` | `pinCode` | camelCase-перевод, совпадает |
| `enable_auto_recording` | `enableAutoRecording` | camelCase-перевод, совпадает |
| `is_recurring` | `isRecurring` | camelCase-перевод, совпадает |
| `description` | `description` | имя совпадает |

Совпадение по двум независимым источникам (сторонний код + официальная спека соседнего
пути) — сильный, но не прямой довод: пути физически разные (`/calendar` без параметра пути,
session-режим vs `/api/EmailCalendar/{email}`, api-key-режим). [emerging — правдоподобная
экстраполяция, не проверено запросом к `/calendar`]

### Ф-39. Схема заметно шире сигнатуры mainpart — не покрытые mainpart поля

Минимум 10 задокументированных полей не имеют аналога в python-сигнатуре `create_meeting`:
`allowAnonymous`, `recurrence` (объект), `simultaneousTranslation`, `customMeetingUrl`,
`maskingSettings`, `autoRunDeepFakeDetection`, `isAllDayEvent`, `optionalUserKeys`,
`requiredExternalAttendeesEmails`, `optionExternalAttendeesEmails`, `controlledViaExternalSystem`.

Существеннее всего — **`recurrence`**. `TalkCalendarRecurrence`:

| Поле | Тип |
|---|---|
| `type` | enum `TalkCalendarRecurrencePatternType`: `notSupported, daily, dailyRegeneration, weekly, weeklyRegeneration, monthly, monthlyRegeneration, relativeMonthly, yearly, relativeYearly, yearlyRegeneration` |
| `startDate` | date-time |
| `endDate` | date-time, nullable |
| `numberOfEvents` | int, 1–100, nullable |
| `interval` | int, 1–99, nullable |
| `dayOfMonth` | int, nullable |
| `daysOfTheWeek` | array enum `TalkCalendarDayOfTheWeek` (`sunday`…`saturday`, `day`, `weekday`, `weekendDay`), nullable |
| `month` | enum `TalkCalendarMonth` (`january`…`december`) |
| `dayOfTheWeekIndex` | enum `TalkCalendarDayOfTheWeekIndex` (`first, second, third, fourth, last`) |
| `firstEventId` | string |

`create_meeting` из mainpart предлагает только булев `is_recurring` — флаг «это повторяющаяся
встреча» без единого параметра паттерна повторения. Если `/calendar` действительно использует ту
же по составу модель, что и `/api/EmailCalendar/{email}`, то **`is_recurring=True` без объекта
`recurrence` не может выразить конкретный шаблон повторения** (еженедельно/ежемесячно/по дням
недели) — что произойдёт на практике (ошибка валидации, встреча без реального повторения,
сервер подставляет умолчание) неизвестно и не может быть выяснено без POST. [primary для формы
`recurrence`; вывод о недостаточности `is_recurring` — логическое следствие, не проверено]

### Ф-40. Оговорка про `required`: `start`/`end`/`timezone` не входят в обязательные

`required: ["requiredUserKeys", "roomName", "subject"]` — даты начала/конца и таймзона в этот
список не входят, хотя логически необходимы для назначения времени встречи. Возможные объяснения
(ни одно не проверено): (а) сервер применяет умолчания для не указанных дат; (б) `required` в этой
спеке декларативен и не соответствует фактической серверной валидации (уже наблюдалось для других
операций — CLAUDE.md, «спеке верить нельзя»); (в) поля обязательны фактически, декларация в спеке
неполна. [primary — как факт декларации; причина — открытый вопрос]

### Ф-41. Формат таймзоны подтверждён дважды дословно

Описание поля `timezone` в спеке: «Временная зона в формате GMT+X». Дефолт в сигнатуре mainpart:
`timezone: str = "GMT+3"`. Это единственное поле тела, где совпадение — не только по имени, но и
по формату значения, между двумя независимыми источниками. Самое надёжное поле среди всех
25+ полей тела. [primary + secondary, скрещено] [established формат по документации; фактическое
поведение сервера при других форматах не проверено]

### Ф-42. Прочие типы и границы из документированной модели

`pinCode` — тип `string` (не число), совпадает с `pin_code: str | None` в mainpart. `subject` —
`maxLength: 128`, `minLength: 0` (граница не встречается в сигнатуре mainpart, там просто `str`).
[primary]

### Ф-43. В спеке нет иной Calendar/Meeting-схемы, которая могла бы описывать именно `/calendar`

Полный список схем компонентов с «Calendar» или «Meeting» в имени (21 штука) исчерпывается двумя
семействами: `EmailCalendar*` (создание/чтение/правка встреч по email организатора, см. выше) и
`CalendarServer*`/`TalkCalendarFeatureSettingsModify` (административная настройка интеграции с
внешним календарным сервером — EWS/EWS Office 365/CalDAV, к созданию встреч отношения не имеет).
Нет «осиротевшей» схемы, которая была бы явно про внутренний `/calendar` без пути к ней. [primary]
[established — отрицательный результат поиска]

## §3. Идемпотентность повторного `POST /calendar` (п.3 постановки) — не исследовалась

Постановка (§5, п.3) прямо выносит этот вопрос за периметр разведки: «физически не проверяется без
записи и вынесен в боевую приёмку с ведома владельца». Красная линия разведки запрещает любые
пробные POST на боевой домен без явного разрешения владельца на конкретный запрос. Вопрос
остаётся открытым и должен быть закрыт отдельным, санкционированным владельцем шагом — вероятно,
как часть критерия приёмки «Боевое создание встречи выполнено один раз, в отдельной тестовой
комнате, с ведома владельца» (§6 постановки), а не разведкой.

## Что НЕ удалось выяснить

- **Точные параметры `GET /api/calendar/events`.** Перебраны `id`/`eventId`/`meetId` как query, оба
  варианта как сегмент пути (используя реальные значения `id`/`meetId` из живых элементов
  `/api/calendar`) — во всех случаях либо неизменное `400 «Неверный идентификатор встречи»`, либо
  `404` (путь-сегмент не матчится ни одним маршрутом). Замечено попутно: тело ошибки этого пути
  структурно богаче, чем у `/api/calendar` — содержит `errorSubType: "CalendarBadRequest"`,
  `errorCode: "CommunicationError"`, `errorDetails`, `exception` (все null здесь), тогда как у
  `/api/calendar` — только `errorId/errorType/errorCode:"ValidationError"/errorMessage`. Два разных
  формата ошибки в рамках одного и того же внутреннего контура (Ф-26 vs здесь) — не выяснено, что
  этот путь вообще делает и нужен ли он для FR-18 (сам `/api/calendar` уже отдаёт полные объекты
  встреч, без необходимости в дозапросе за деталями одного события).
- **Причина широты выдачи `/api/calendar` (Ф-30).** Домен-вайд ли это по конструкции, или эффект
  прав конкретного тестового аккаунта — не проверено вторым, менее привилегированным аккаунтом
  (недоступен для зонда).
- **Какой scope (если вообще) реально проверяется на `/api/calendar` под api-key (Ф-34).** Три
  родственных операции под одним и тем же ключом дали три разных результата (200 / 403 / 403) —
  причина расхождения не выяснена ни экспериментом, ни документацией.
- **Всё содержимое §2 не проверено запросом.** Форма тела `POST /calendar` — экстраполяция с
  соседнего пути, не факт. В частности: реагирует ли сервер на `isRecurring: true` без
  `recurrence`; действительно ли `start`/`end`/`timezone` не обязательны на практике, несмотря на
  документированный `required`; принимает ли `/calendar` вообще те же имена полей (camelCase),
  что и `/api/EmailCalendar/{email}`, а не какие-то другие.
- **Идемпотентность повторного POST (п.3 постановки)** — намеренно не исследовалась, см. §3.

## Рекомендации для BA/SA

- **BA:** формулируя FR-18 (чтение календаря), учесть находки Ф-20/Ф-21/Ф-22 при определении
  критериев приёмки для окна дат и постраничности — 7-дневный потолок с явным `end`, отсутствие
  постраничности сверх первых ≤100 элементов при `take`, отсутствие рабочего `skip`.
- **BA:** формулируя FR-18, учесть Ф-30 — выдача может оказаться не «личным календарём» вызывающего
  пользователя, а более широкой (доменной) выборкой; критерии приёмки не стоит формулировать как
  «пользователь видит свой календарь», пока это не подтверждено на не-административном аккаунте.
- **BA/SA:** для FR-13 (планирование встречи) явно решить, что делать с полем `is_recurring`/
  `isRecurring` без объекта `recurrence` (Ф-39) — либо волна ограничивается однократными встречами
  (`is_recurring` не выставляется вовсе), либо требование должно включать полный объект `recurrence`
  с самого начала, а не булев флаг «на будущее».
- **SA:** при проектировании `OPERATION_PROFILES` учесть асимметрию Ф-33/Ф-34/Ф-35 — три
  родственных пути под одним и тем же api-key-режимом ведут себя по-разному (403/200/403). Прежде
  чем декларировать профиль для `calendar-read` в api-key-режиме как «недоступно», стоит явно
  зафиксировать в ADR, на каком именно пути и почему (внутренний `/api/calendar`, не документированный
  аналог) он оказался доступен, и не полагаться на этот эффект молча — поведение недокументированного
  пути может измениться без предупреждения (тот же риск Р-3 постановки).
- **SA:** тело `POST /calendar` (§2) — потолок доверия «экстраполяция с соседнего документированного
  пути, дважды подтверждённая для общего подмножества полей» (Ф-38, Ф-41), не факт. Решение о
  риске (реализовать по экстраполяции vs дождаться боевой проверки конкретных полей) — предмет
  ADR, не этого документа.
- **SA:** отдельно зафиксировать в ADR наблюдение Ф-28 — внутренний `/api/calendar` не идентичен по
  форме элемента задокументированной `EmailCalendarItem` (два поля вне спеки, несколько
  документированных полей не встретились ни разу) — маппер стоит строить по факту выдачи, не по
  спеке буквально.

## §4. Боевая приёмка 0.6.0 — живая разведка чтения (RES-001, 2026-08-17)

**Запрос PM:** RES-001 из `docs/postanovka-wave-2.1-boevaya-priyomka.md` — живые `get_room` (реальная
комната `vkuznetsov` + заведомо отсутствующее имя), календарь на окне >7 дней (клиентская нарезка и
дедуп), окно с ≥100 элементами в сегменте (если найдётся), принадлежность выдачи календаря.
**Глубина:** quick (≤30 мин). **Метод:** прямой вызов `ktalk_mcp.rooms.get_room` /
`ktalk_mcp.calendar_reader.get_calendar_window` / `ktalk_mcp.calendar_reader.split_window` через
`KTalkClient.from_settings(Settings())`, session-режим (`auth_mode: AuthMode.SESSION`, подтверждено
программно). Скрипты зонда — во временной директории сессии, не входят в артефакт.

### Ф-44. `get_room("vkuznetsov")` — 200, состав полей совпадает с ROOM_FIELDS без остатка

Сырой ответ и маппер (`map_room`) дают идентичный набор из 18 ключей — тот же список, что
`ROOM_FIELDS` в `rooms.py` (§4.2 спеки): `allowAnonymous, anonymousAccessExpirationDate,
anonymousAccessModifiedDate, anonymousModerators, audioPolicy, chatChannelSettings, conferenceId,
isModerator, maskingSettings, moderators, onlineUsers, roomName, screenSharePolicy, sessionHalls,
simultaneousTranslation, sipSettings, stageConferenceId, videoPolicy`. Лишних полей сверх
задекларированных 18 в сыром ответе нет; `require_contract_field("roomName")` не сработал бы иначе.
Для `vkuznetsov`: `moderators: []`, `onlineUsers: []`, `allowAnonymous: true`,
`anonymousAccessExpirationDate` — момент запроса +24 часа. [primary] [established]

### Ф-45. `get_room` на заведомо отсутствующем имени — тоже 200, не 404 (разблокирует FR-17 AC2)

```
GET /api/room/vkuznetsov-nonexistent-probe-0001?sessionToken=<REDACTED>
→ 200, {"roomName":"vkuznetsov-nonexistent-probe-0001", "moderators":[], "onlineUsers":[],
        "allowAnonymous":true, "anonymousAccessExpirationDate":<момент запроса +24ч>, ...}
```

Структурно и по значениям (кроме `roomName`/`stageConferenceId`/`conferenceId`, которые включают
переданное имя) ответ идентичен Ф-44: `moderators: []`, `onlineUsers: []`, `allowAnonymous: true`,
`isModerator: true`, `anonymousAccessExpirationDate` — тоже момент запроса +24 часа. Сервер не
различает существующую и заведомо не создававшуюся комнату кодом ответа — оба случая дают 200 с
одинаковой по форме заготовкой (похоже на комнату, синтезируемую по имени на лету, а не на 404 для
отсутствующей сущности). Спека (`rooms-calendar-spec.md`, §4.2) прямо называла этот исход
гипотетическим и не проверенным живьём («поведение для несуществующей комнаты не проверено») —
теперь проверено, отрицательный результат: `get_room` не может использоваться как индикатор
существования комнаты, GET не различает «есть» и «нет». Наш код (`get_room` в `rooms.py`) сегодня
одинаково маппит оба случая, диагностика дрейфа (`diagnose_undocumented_failure`) не срабатывает,
поскольку код ответа 200 в обоих случаях. [primary] [established]

### Ф-46. Календарь на окне 14 дней (2026-08-17…2026-08-30) — нарезка на 2 сегмента, без дублей на стыке

`split_window(2026-08-17, 2026-08-30)` → `[(2026-08-17, 2026-08-23), (2026-08-24, 2026-08-30)]` —
два смежных 7-дневных сегмента без разрыва и без перекрытия (второй начинается на день позже конца
первого). Сырые счётчики по сегментам: первый — 17 элементов, второй — 16. `CalendarReadResult`:
`items` — 33 (после дедупа по `id`), `incomplete_segments` — пусто (оба сегмента < 100). Сумма сырых
count (17+16=33) совпадает с итоговым числом после дедупа — на этом конкретном окне дублей на стыке
не возникло (сегменты по датам не пересекаются, так что дедуп-логика тут не имела повода сработать;
проверяет лишь то, что нарезка не роняет и не задваивает элементы механически, а не что ключ дедупа
работает на пересекающихся датах). [primary] [established]

### Ф-47. Потолок сегмента (≥100 элементов) не найден в домене — 16 недель просканировано, максимум 17

Просканированы 16 последовательных недельных окон вперёд от 2026-08-17 до 2026-12-06 (несмежные с
Ф-46 запросы, тот же метод: `take=100` на 7-дневный сегмент). Диапазон по неделям — 13…17 элементов,
максимум 17 (окна 2026-08-17, 2026-09-07, 2026-10-05, 2026-10-19, 2026-11-16). Ни одна неделя не
приблизилась к потолку 100. Окна с ≥100 элементами в сегменте в этом домене найти не удалось —
поведение при достижении `_PAGE_SIZE` (предупреждение `incomplete_segments` вместо тихого усечения)
осталось непроверенным живым ответом; логика проверена только модульно (`incomplete = len(items)
>= _PAGE_SIZE` в `calendar_reader.py`), не сквозным запросом. [primary] [established про факт
ненайденности; поведение на потолке — не проверено]

### Ф-48. Выдача календаря — не личный календарь `vkuznetsov`, подтверждает Ф-30 прямым тестом

Окно Ф-46 (33 элемента, 14 организаторов, 20 различных `roomName`) разобрано на присутствие
`vkuznetsov` (по `organizer.user.login`, `organizer.mailbox`, и то же для `requiredAttendees`/
`optionalAttendees`): `vkuznetsov` — организатор в 0 из 33 элементов, участник (обязательный или
опциональный) — тоже в 0 из 33. Сессия, которой выполнялся запрос, принадлежит другому пользователю
(не `vkuznetsov`) и не связана с ним ни в одном из 33 элементов окна, но видит их все. Это прямое
подтверждение Ф-30 прошлой волны (тогда — только по совпадению «26 организаторов / 26 комнат»,
здесь — по явной проверке участия конкретного известного пользователя): `/api/calendar` не
фильтрует по принадлежности вызывающей сессии, выдача шире, чем «мой календарь». Недоказуемо
без второго аккаунта: связана ли эта широта с правами конкретного тестового аккаунта (административный
или иной привилегированный статус) или это общее свойство эндпоинта для любой сессии — сравнить
не с чем, доступен один аккаунт. [primary] [established: `vkuznetsov` отсутствует в выдаче как
участник; интерпретация причины широты — по-прежнему [emerging], как и в Ф-30]

### Ф-49. Развилка Ф-45 закрыта: (Б) — GET создал персистентный объект, не (А) вычисление на лету

PM запросил уточнение: отличить «эндпоинт детерминированно вычисляет ответ по имени, ничего не
создавая» (А) от «GET создал объект как побочный эффект» (Б). Три проверки, все методом GET:

**1. Повторный `get_room("vkuznetsov-nonexistent-probe-0001")`, спустя интервал внутри той же
задачи.** Побайтовое сравнение с сохранённым первым ответом (`room-nonexistent-raw.json`):
`stageConferenceId` и `conferenceId` — идентичные хеш-суффиксы в обоих ответах. Решающий признак —
`anonymousAccessExpirationDate`/`anonymousAccessModifiedDate`: первый ответ —
`2026-08-18T09:26:03.1832636Z` / `2026-08-17T09:26:03.1832642Z`; повторный, снятый позже, — та же
секунда, `...09:26:03.183Z` в обоих полях (различие только в точности сериализации дробных секунд,
не в значении). Если бы сервер пересчитывал «сейчас + 24 часа» на каждый GET, повторный ответ
показал бы более позднее время — этого нет. Все остальные поля (включая `moderators: []`,
`onlineUsers: []`, `sipSettings`, `chatChannelSettings`, `maskingSettings`) совпали дословно.

**2. Свежее случайное имя, ранее не запрошенное (`probe-fresh-7d2a4860aac4a5b7`), и повтор того же
имени спустя ≥65 секунд.** Первый GET дал `anonymousAccessExpirationDate: 2026-08-18T09:30:52.5455841Z`
(момент того запроса). Повторный GET того же имени 65+ секунд спустя — `2026-08-18T09:30:52.545Z`:
та же секунда первого запроса, не время повторного (которое было бы на 65+ секунд позже). Вывод (1)
воспроизведён на имени, гарантированно не встречавшемся в контуре до этой задачи.

**3. Второе случайное имя (`probe-fresh2-cf44d841c9aa6fb7`)** — тот же паттерн ответа (200,
`moderators: []`, `allowAnonymous: true`, хеш-подобные `conferenceId`/`stageConferenceId`,
детерминированные от имени), подтверждает: это не особенность одного конкретного имени, а общее
поведение эндпоинта на любое ранее не запрошенное имя.

**4. Путь листинга/подтверждения существующих комнат чтением.** В `talk.public.api-api-2.json`
все 8 путей с «room» в имени параметризованы `{roomName}` (`GET/PUT /api/Rooms/{roomName}`,
`POST /api/Rooms/{roomName}/...`, `GET /api/RoomReport/{roomName}/statistics/report`,
`GET /api/RoomReport/statistics`) — коллекционного `GET /api/Rooms` без параметра в спеке нет.
В `auth.py::OPERATION_PROFILES` зарегистрирована только одна операция чтения комнаты — `get_room`
по единственному имени (`AuthMode.SESSION: EndpointProfile("/api/rooms/{room_name}", None)`),
операции листинга нет. Иного способа читать список существующих комнат в проекте и в спеке не
найдено.

**Вывод: (Б), не (А).** `GET /api/rooms/{room_name}` на ранее не запрошенное имя создаёт (или
как минимум персистентно кеширует с фиксацией времени создания) объект комнаты как побочный эффект
самого чтения — `anonymousAccessExpirationDate`/`anonymousAccessModifiedDate` фиксируются в момент
первого GET и не пересчитываются при повторных GET, что исключает чистое вычисление на лету.
**Зонд оставил в боевом контуре как минимум три таких объекта:**
`vkuznetsov-nonexistent-probe-0001`, `probe-fresh-7d2a4860aac4a5b7`, `probe-fresh2-cf44d841c9aa6fb7`
— все три созданы исключительно GET-запросами в рамках этой и предыдущей задачи RES-001, ни одного
POST/PUT не выполнялось. Ни один из этих объектов не был явно удалён (инструмента удаления комнаты
в проекте нет; в постановке волны 2.1 удаление комнат не упоминается). Метода чтением подтвердить
или опровергнуть существование комнаты до GET (перечисление существующих комнат) в контуре нет —
п.3 запроса закрыт отрицательно. [primary] [established]

### Расхождения с фикстурой `tests/fixtures/room-detail-session.json`

Форма (не только значения) четырёх вложенных полей в фикстуре не совпадает с живым ответом:

| Поле | Фикстура | Живой ответ (Ф-44, Ф-45) |
|---|---|---|
| `onlineUsers` | `0` (число) | `[]` (массив) |
| `simultaneousTranslation` | `false` (булево) | `{"languagePairs": []}` (объект) |
| `sipSettings` | `{"enabled": true, "pinCode": "0000"}` | `{"sipNumber": "", "sipUri": "", "phoneNumbers": []}` |
| `chatChannelSettings` | `{"general": {"enabled": true}}` | `{"enabled": true}` |
| `maskingSettings` | `{"enabled": false}` | `{"nameMaskingMode": "none", "postMaskingMode": "none", "showAdditionalInfo": true}` |

Спека (`rooms-calendar-spec.md`, §4.2) заранее не утверждала форму этих вложенных полей —
«их внутренняя форма не документирована и зондом отдельно не проверялась» — поэтому это не
опровержение спеки, а факт для сверки фикстуры (задача QA-001). [primary] [established]

### Что не удалось проверить в этом заходе

- Поведение на потолке сегмента (≥100 элементов, Ф-47) — окна не нашлось, живой ответ на превышение
  не получен.
- Дедуп на реально пересекающихся датах на стыке сегментов — окно Ф-46 такого случая не создало
  (сегменты `split_window` по построению не пересекаются); проверка самой ключевой функции дедупа
  на дублирующихся `id` живым ответом не проводилась (только по модульным тестам кода).
- Причина широты выдачи календаря (админ-статус аккаунта vs общее свойство эндпоинта) — тот же
  пробел, что в Ф-30: нужен второй, менее привилегированный аккаунт, недоступен для зонда.
- Идемпотентность `POST /calendar` — вне периметра этой задачи (RES-001 — только чтение), выносится
  в DEV-002 постановки.
- Не проверено, можно ли отменить побочный эффект Ф-49 без записи (например, TTL-подобное
  самоистечение) — `anonymousAccessExpirationDate` у всех трёх оставленных объектов истекает
  2026-08-18, то есть в пределах суток от момента зонда; удалось ли за счёт этого объекту
  «исчезнуть» естественным путём, повторным GET после этой даты не проверялось (задача закрыта
  до наступления срока).

## Источники (дополнение §4)

- [primary] Живые вызовы `ktalk_mcp.rooms.get_room`, `ktalk_mcp.calendar_reader.get_calendar_window`,
  `ktalk_mcp.calendar_reader.split_window` через `KTalkClient.from_settings(Settings())`,
  session-режим, выполненные 2026-08-17 в рамках боевой приёмки 0.6.0
  (`docs/postanovka-wave-2.1-boevaya-priyomka.md`, RES-001). Сырые ответы — во временной директории
  сессии PM, не входят в артефакт (содержат ФИО/email участников — PII).
- [primary] `tests/fixtures/room-detail-session.json` — сравнение форм вложенных полей.
- [primary] `content/40-architecture/rooms-calendar-spec.md`, §4.2, 5.1 — сверка с ранее
  задекларированным контрактом.
- [primary] Повторные живые GET-и той же сессией по уточняющему запросу PM (Ф-49): повтор
  `get_room("vkuznetsov-nonexistent-probe-0001")`, два свежих случайных имени и повтор одного из
  них после паузы ≥65 секунд. Выполнены 2026-08-17, только метод GET. Сырые ответы — во временной
  директории сессии PM.
- [primary] `talk.public.api-api-2.json` — полный перечень путей с «room» в имени (проверка
  отсутствия коллекционного эндпоинта листинга, Ф-49 п.4).
- [primary] `src/ktalk_mcp/auth.py::OPERATION_PROFILES` — перечень зарегистрированных операций
  чтения комнаты (Ф-49 п.4).

## §5. Браузерный HAR (2026-08-17)

**Запрос PM:** срочная фиксация фактов из HAR-снимка живого браузерного трафика перед закрытием
сессии. **Метод:** факты извлечены владельцем из локального HAR-файла (в `docs/`, в `.gitignore`,
не входит в репозиторий и не читался этим агентом — файл содержит перс. данные) и переданы в
задаче текстом; здесь только структурирование и нумерация. **Глубина:** quick.

### Ф-50. Отмена встречи — `POST /api/calendar/{id}/cancel`

```
POST /api/calendar/{id}/cancel
Тело: {"reason": ""}
→ 200
```

`{id}` — тот же base64-идентификатор Exchange, что приходит в поле `id` элемента ответа на
создание встречи. До этой находки в проекте считалось, что операции отмены/удаления встречи нет
вовсе. [primary] [established]

### Ф-51. Правка встречи — `PUT /api/calendar/{id}`

```
PUT /api/calendar/{id}
→ 200
```

Форма тела в рамках этой задачи не разбиралась — открытый вопрос для следующей сессии.
[primary] [established: сам факт существования операции; форма тела не выяснена]

### Ф-52. Форма тела создания встречи подтверждена вторым независимым живым образцом

Второй образец — другая встреча, другая комната, другое время. Набор ключей тела совпадает с
первым снимком (§2, Ф-37/Ф-38) один в один, 14 штук: `subject, description, start, end, timezone,
roomName, allowAnonymous, anonymousAccessExpirationDate, pinCode, enableAutoRecording,
autoRunDeepFakeDetection, isRecurring, requiredAttendees, maskingSettings`.

Значения второго образца: `timezone: "GMT+3"`, `pinCode: null`, `autoRunDeepFakeDetection: null`,
`isRecurring: false`; `start`/`end` — UTC с суффиксом `Z` и миллисекундами; `maskingSettings` —
объект с ключами `nameMaskingMode`/`postMaskingMode`/`showAdditionalInfo`; `requiredAttendees` —
массив объектов ровно с двумя ключами `type` и `key`.

Переводит форму тела `POST /calendar` из статуса «известна из одного снимка/документарной
экстраполяции» (Ф-37…Ф-39, `[emerging]`) в «воспроизведена дважды живым трафиком браузера на
разных встречах» — повышает доверие к перечню из 14 ключей и их значениям, но не отменяет открытый
вопрос §2 про поля сверх этих 14 (`recurrence`, `simultaneousTranslation` и др. из
`CreateEmailCalendarEventModel`) — они в живых образцах не наблюдались ни разу, значит либо не
используются клиентом Толка, либо опциональны и молча опускаются. [primary] [established для
14-ключевого подмножества; о полноте относительно полной документированной модели — по-прежнему
не выяснено]

### Ф-53. Справочник контактов — резолюция текстового поиска в числовой id

```
GET /api/contacts?query=<текст>&top=25&fillInMeetingStatus=false&includeKiosks=true
→ 200 {"contacts": [ {key, firstname, surname, patronymic, post, userType, contactType,
                       hasEmail, hasMobilePhone, mobilePhone, avatarUrl}, ... ]}
```

`key` — тот самый числовой идентификатор, ожидаемый в `requiredAttendees[].key` тела создания
встречи (Ф-52). `post` — корпоративный адрес контакта.

Наблюдённое поведение поиска по подстроке (обезличено): запрос из 7 букв с опечаткой → 0
совпадений; корректная 6-буквенная основа фамилии → 16 совпадений; фамилия плюс начало имени →
1 совпадение. Поиск, таким образом, ищет по тексту имени/фамилии, сужается точным совпадением, и
чувствителен к опечаткам (не делает нечёткого/фонетического сопоставления, по крайней мере на этом
образце). [primary] [established]

**Опровергает ранее принятое решение.** ADR-009 §3 (в более ранней версии этой заметки ошибочно
указывалось «п.4» — исправлено; п.4 ADR-009 про `roomName`, к резолюции id не относится) фиксировал
отсутствие операции «логин пользователя → числовой id» как **подтверждённый пробел** и на этом
основании принимал решение вводить числовой id участника вручную оператором. Эта находка показывает,
что операция резолюции текста в числовой id **существует** (`GET /api/contacts`) — пробела в API
нет. Оговорка: поиск устроен по тексту имени/фамилии, не по логину напрямую — прямое сопоставление
«логин → запись справочника» этим наблюдением не проверялось, различаются может ли `query`
принимать сам логин как текст. Пересмотр выполнен —
[ADR-010](../../00-project/adr/ADR-010-contacts-resolution.md). [primary] [established: операция
существует; established, но с оговоркой: связь именно с логином не проверена]

### Ф-54. Экспорт HAR из Chrome вырезает авторизацию

В снятом HAR-файле нет ни заголовка `Authorization`, ни cookie — Chrome в использованном режиме
экспорта эти данные вычищает. Заголовок `X-Platform: web` присутствует и сохранён. Транспорт
авторизации (какой именно заголовок/токен уходит в запросах §5) взят не из этого файла, а из более
раннего снимка через Web Inspector. [primary] [established — методологическая заметка о самом
источнике, не о API]

## Источники (дополнение §5)

- [primary] HAR-снимок живого браузерного трафика, 2026-08-17, извлечён и обезличен владельцем;
  сам файл — `docs/*.har` (в `.gitignore`, не в репозитории, не читался агентом) — источник
  Ф-50…Ф-54.
- [primary] `content/00-project/adr/ADR-009-devtools-body-and-transport-correction.md` §3 —
  пересмотренное решение (Ф-53); пересмотр закрыт
  `content/00-project/adr/ADR-010-contacts-resolution.md` (SA-010). Сверка файла выполнена — ссылка
  на несуществующий `SA-004-...` была ошибкой более ранней версии этой заметки, здесь исправлена.

## §6. Пятый боевой POST — первая созданная встреча (2026-08-18)

**Запрос PM:** пятая санкционированная боевая попытка `create_meeting` после коррекции ADR-009.
**Метод:** запуск `ktalk create-meeting-confirm` владельцем в TTY (барьер ADR-005), вывод передан
текстом. **Глубина:** факт исхода, не разведка.

### Ф-55. `POST /api/calendar` → 200, встреча создана

Первый успешный мутирующий запрос проекта за всю волну. Конфигурация запроса — ровно та, что
зафиксирована ADR-009: заголовки `Authorization: Session <token>` + `X-Platform: web`, **без**
query `?sessionToken=`; тело из 13 полей компоновщика (`enableSip` отсутствует,
`requiredAttendees: [{"type": "user", "key": "<числовой id>"}]`, `start`/`end` — UTC с `Z`,
`isRecurring`/`autoRunDeepFakeDetection`/`maskingSettings` — архитектурные литералы); куки
браузерной сессии (`kontur_ngtoken`, `ngtoken`) **не воспроизводились**.

Следствия, снимающие статус `ГИПОТЕЗА` с ADR-009: транспорт мутирующих операций без query
работоспособен; 13-полевое тело принимается сервером; **куки для создания встречи не требуются** —
этот пробел, названный в ADR-009 «Consequences» как возможная причина отказа, закрыт отрицательно.
Комната `priyomka-0-6-0-test` создана попутно самим POST'ом (согласуется с ADR-006). [primary]
[established]

### Ф-56. Идентификатор созданной встречи — base64-идентификатор Exchange

Ответ на создание содержит идентификатор той же формы, что `{id}` в Ф-50/Ф-51 (отмена и правка).
Значение — base64 с символами `+`, `/`, `=`; при подстановке в путь требует URL-квотирования
(`quote_path_param`, `auth.py`), иначе путь отмены собьётся. Само значение — принадлежность
календаря владельца, в статьях не приводится. [primary] [established]

### Методологическая заметка: расхождение токенов между процессами одной машины

Четыре отказа предшествующей сессии диагностировались как отказ контура, тогда как минимум
последний из них (401 и на записи, и на контрольном вызове ADR-004) вызван протухшим значением
`KTALK_SESSION_TOKEN`, экспортированным в окружении конкретного терминала. Процессы на одной
машине с одинаковым `base_url` и одинаковой длиной токена держали **разные** значения; сравнение
по длине оказалось ложным признаком тождества, надёжный — отпечаток `sha256[:12]`. Контрольный
вызов ADR-004 при этом отработал по назначению: его собственный отказ и был признаком, что
credential невалиден, а отказ не специфичен для записи. Это заметка об операционной гигиене
замера, не факт контура.

## Источники (дополнение §6)

- [primary] Вывод `ktalk create-meeting-confirm`, запуск владельцем 2026-08-18, передан текстом —
  источник Ф-55, Ф-56.

## §7. Боевая отмена встречи — две попытки (2026-08-19)

**Запрос PM:** проверка `cancel_meeting` на встрече, созданной пятым POST. **Метод:** запуск
`ktalk cancel-meeting-confirm` владельцем в TTY (барьер ADR-005), вывод передан текстом; исход
каждой попытки проверен независимым чтением календаря (`GET /api/calendar`, окно 19.08).
**Глубина:** факт исхода.

### Ф-57. Отмена выполняется и отвечает 200 с ПУСТЫМ телом

Вторая попытка завершилась исключением разбора ответа (`Expecting value: line 1 column 1`), при
этом операция **выполнена**: чтение календаря до попытки дало 6 событий на 19.08, после — 5,
встреча в комнате `priyomka-0-6-0-test` исчезла. Сервер вернул 200 без тела; код делал
безусловный `response.json()` и докладывал успешную необратимую операцию как «исход неизвестен».

Ф-50 фиксировала только код ответа — про наличие тела там не сказано ничего, и это отличие от
`create_meeting`, чей ответ несёт объект встречи (Ф-55). Дефект наш, не контура; исправлен в
`meeting_scheduling.cancel_meeting` с регрессионным тестом. [primary] [established]

### Ф-58. `{id}` валидируется сервером — контраст с `roomName`

Первая попытка ушла с идентификатором, потерявшим при копировании завершающий `=` (203 символа
вместо 204). Ответ — `400`, `errorType: BadRequest`, `errorSubType: CalendarBadRequest`,
`errorCode: CommunicationError`, `errorMessage: «Неверный идентификатор встречи»`. Встреча
осталась в календаре, побочных объектов не создано.

Это прямая противоположность поведению `roomName` в `GET /api/rooms/{roomName}`, который
принимает любую строку и молча создаёт объект (ADR-006): у идентификатора встречи валидация
есть, и неверное значение безопасно. Контроль авторизации при этом прошёл — 400 с телом означает,
что credential валиден, и перепроверять токен на таком отказе не нужно. [primary] [established]

### Ф-59. Форма `{id}` в живой выдаче — уточнение Ф-56

Ф-56 описывала идентификатор как «base64 с символами `+`, `/`, `=`». Прямой замер выдачи
календаря на 6 событиях: у четырёх идентификаторов нет ни одного из трёх символов, у двух есть
только `=`; `+` и `/` не встретились ни разу. Длины — 204 и 224 символа.

Формулировка Ф-56 переносила на внешний вид идентификатора свойства его внутреннего слоя
(идентификатор декодируется в другую base64-строку, где `+` и `=` действительно есть) — сервер
же и отдаёт, и принимает именно внешний вид, декодировать ничего не нужно. `quote_path_param`
остаётся обязательным из-за `=`. [primary] [established на выборке из 6 событий одного домена]

## §8. Правая граница окна `GET /api/calendar` (RES-004)

Замер живьём на реальных данных боевого аккаунта 19-21.08.2026, прямыми `GET`-запросами мимо
`get_calendar_window`. Ни один факт из §1 (Ф-17…Ф-27) не оспаривается — новые факты уточняют то,
что там не было измерено (правая граница, часовой пояс, точная формула 7-дневного лимита).

### Ф-60. Правая граница окна `end` — полуоткрытая, исключает сам день `end`

```
start=2026-08-19&end=2026-08-19 → 200, items=[]
start=2026-08-19&end=2026-08-20 → 200, 5 элементов, все с start в пределах 19.08
start=2026-08-19&end=2026-08-21 → 200, 12 элементов: 5 за 19.08 + 7 за 20.08
```

Каждое увеличение `end` на календарный день добавляет ровно один календарный день выдачи, но
день, равный `end`, в выдачу не попадает никогда. Левая граница `start`, напротив, включительна —
элементы `start`-дня присутствуют начиная с первого запроса. Итоговая формула: `[start 00:00,
end 00:00)` — полуоткрытый интервал, а не «включительно с обеих сторон», как можно было
предположить по формулировке Ф-18 (там зафиксирован только код 200 и число элементов, без
разбивки по датам). Прямое следствие для CLI: `--start D --end D` детерминированно возвращает
пустой список даже при наличии встреч в этот день — это не сбой, а работающая по спецификации
полуоткрытость, к которой сегментация должна адаптироваться отдельно. [primary] [established]

### Ф-61. Пропажа 22-23.08 в диапазоне 17-23 объясняется отсутствием данных, не полуоткрытостью окна

Боевое наблюдение: `--start 2026-08-17 --end 2026-08-23` вернул дни 17-21, без 22-23. Прямой
узкий зонд той же выдачи:

```
start=2026-08-21&end=2026-08-23 → 200, 3 элемента, все за 21.08
start=2026-08-22&end=2026-08-23 → 200, items=[]
```

`22-23` пусты при запросе строго в их границах — не только в широком окне. Полуоткрытость (Ф-60)
объясняет отсутствие 23-го (граница `end`), но не объясняет отсутствие 22-го: 22-е должно было
попасть в `[17 00:00, 23 00:00)`. Прямой узкий запрос `22→23` показывает, что на 22.08 в аккаунте
физически нет встреч — совпадает с независимым запросом на более широкое окно `17→24`, дающим
тот же набор из 22 элементов, дни 17-21, без изменений от расширения. Вывод: пропажа 22-23 —
свойство календаря оператора на момент замера (2026-08-19), не дефект окна. [primary]
[established]

### Ф-62. Сервер принимает явное время в `end`, но часовой базис границы неотличим по имеющимся данным

```
end=2026-08-19T23:59:59            → 200, тот же набор из 5 элементов, что и bare-дата end=19
end=2026-08-19T23:59:59+03:00      → 200, тот же набор
end=2026-08-19T21:00:00Z           → 200, тот же набор
end=2026-08-19T20:59:00Z / T21:01:00Z → 200, тот же набор (5 элементов)
```

Явное время не отклоняется валидацией (нет ошибки формата) и не ломает выдачу — сервер принимает
и `T23:59:59` без зоны, и с `+03:00`, и `Z`. Определить точный часовой базис границы `[start
00:00, end 00:00)` — UTC-полночь или MSK-полночь — на имеющихся данных нельзя: между последней
встречей суток (макс. `15:00Z` 19.08, `14:00Z` 18.08) и первой встречей следующих суток (мин.
`08:30Z` 20.08) нет ни одной встречи, то есть обе гипотезы (`00:00 UTC` и `21:00Z`≡`00:00 MSK`)
попадают в один и тот же пустой промежуток и не различимы этим замером. Элементы выдачи сами
всегда в `Z` (UTC) — Ф-59/Ф-28 это уже отмечали для формы поля, здесь то же подтверждено для §8.
[primary] [established — факт принятия времени; contested/не удалось выяснить — какая именно
полночь служит границей]

### Ф-63. 7-дневный лимит — по разнице `end − start`, не по числу календарных дат; с полуоткрытым `end` это даёт ровно 7 суток покрытия

```
start=2026-08-17&end=2026-08-24 (разница 7 дней, 8 календарных дат start..end) → 200
start=2026-08-17&end=2026-08-23 (разница 6 дней, 7 календарных дат)             → 200, тот же набор
start=2026-08-17&end=2026-08-23T23:59:59 (разница 6д 23:59:59)                  → 200, тот же набор
start=2026-08-17&end=2026-08-25 (разница 8 дней)                                → 400 «Период запроса
                                                                                     не должен превышать 7 дней»
start=2026-08-17&end=2026-08-26/27/31 (разница 9, 10, 14 дней)                  → тот же 400
```

Порог — `(end − start).days ≤ 7`, независимо от того, сколько календарных дат при этом
затрагивается. Согласуется с Ф-20 (там пример «ровно 7 дней» `08-01→08-08`, разница тоже 7, тоже
200) — новых фактов, противоречащих Ф-20/Ф-26, не найдено, только уточнение механизма. Из-за
полуоткрытости `end` (Ф-60) максимально допустимый запрос с разницей 7 дней **фактически
покрывает ровно 7 суток данных** (`17.08 00:00` … `24.08 00:00` исключительно = дни 17-23), а не
8. `split_window(max_days=7)` в `calendar_reader.py:56-68` строит сегменты `[seg_start,
seg_start+6]` и передаёт `seg_end` в параметр `end` как есть — при подставленной сюда семантике
включительного дня это на один день *меньше* максимально допустимого сервером диапазона (сервер
пропустил бы разницу 7, сегментация запрашивает разницу 6), но одновременно, из-за полуоткрытости
самого `end`, реального дня `seg_end` в выдаче нет вовсе — тот день должен запрашиваться уже
следующим сегментом. [primary] [established]

### Ф-64. Вырожденный и обратный интервал — разное поведение, оба воспроизведены

```
start=2026-08-19&end=2026-08-19 → 200, items=[]  (не 400 — валидатор допускает start==end)
start=2026-08-20&end=2026-08-19 → 400 «Дата окончания должна быть больше даты начала»
```

Совпадает дословно с текстом, уже занесённым в `KNOWN_400_TEXTS` и в Ф-26 — подтверждение, не
новый текст. [primary] [established]

## Источники (дополнение §8)

- [primary] Живые `GET`-запросы к `/api/calendar` в session-режиме, минуя `get_calendar_window`,
  выполненные напрямую через `KTalkClient` этого репозитория 2026-08-19 в рамках RES-004 —
  источник Ф-60…Ф-64. Скрипты зонда — во временной директории сессии, не входят в артефакт; токен
  и домен в статью не переносятся.
- [primary] Боевое наблюдение оператора 2026-08-19 (`ktalk list-calendar` на трёх диапазонах,
  сведено в таблицу постановки RES-004) — исходная аномалия, разрешённая Ф-60/Ф-61.

## Источники (дополнение §7)

- [primary] Вывод `ktalk cancel-meeting-confirm`, две попытки владельца 2026-08-19, переданы
  текстом — источник Ф-57, Ф-58.
- [primary] `GET /api/calendar` на окно 19.08.2026, три чтения (до попыток, между ними и после) —
  контроль исхода и источник Ф-59. Идентификаторы встреч в статью не переносятся.

## Источники

- [primary] `talk.public.api-api-2.json` (локальный файл в корне worktree
  `/Users/mdemyanov/Devel/ktalk-mcp/.worktrees/epic-rooms-calendar/talk.public.api-api-2.json`) —
  единственный документарный источник схемы `CreateEmailCalendarEventModel` и семейства
  Calendar-путей/схем (§2, Ф-36…Ф-43).
- [primary] Живые GET-запросы к `https://naumen.ktalk.ru/api/calendar` и смежным путям,
  session-режим (`?sessionToken=`) и api-key-режим (`X-Auth-Token`), выполненные в рамках этого
  исследования 2026-08-13 — источник §1, Ф-17…Ф-36. Скрипты зонда — во временной директории
  сессии, не входят в артефакт.
- [secondary] `content/10-domain/research/mainpart-ktalk-mcp.md` — сигнатура `create_meeting`
  чужого MCP-сервера, разобранная прошлой волной по исходному коду `server.py`; используется как
  сравнение, не как источник истины (Ф-38, Ф-39).
- [secondary] `docs/postanovka-wave-2-rooms-calendar.md`, §5 — формулировка задачи и уже известные
  на момент постановки факты (не переоткрывались).
- [primary] `content/10-domain/research/ktalk-integrator-api.md` (RES-001) — находка 8 про
  `access-info` как кандидат whoami; статус этой находки пересмотрен здесь (Ф-35).
